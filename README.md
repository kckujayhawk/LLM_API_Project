We found SMOTE was artificially boosting recall but flooding the model with false positives, so we removed it and shifted to a precision-first ranking approach—allowing us to capture a higher share of conversions with significantly less mail volume.
